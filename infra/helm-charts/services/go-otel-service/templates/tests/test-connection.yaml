apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-otel-service.fullname" . }}-test-connection"
  labels:
    {{- include "go-otel-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-otel-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
