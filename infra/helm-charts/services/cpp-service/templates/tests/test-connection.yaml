apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cpp-service.fullname" . }}-test-connection"
  labels:
    {{- include "cpp-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cpp-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
