apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpp-otel-service.fullname" . }}
  labels:
    {{- include "cpp-otel-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpp-otel-service.selectorLabels" . | nindent 4 }}
