apiVersion: v1
kind: Service
metadata:
  name: {{ include "rust-otel-service.fullname" . }}
  labels:
    {{- include "rust-otel-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rust-otel-service.selectorLabels" . | nindent 4 }}
