apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-otel-service.fullname" . }}
  labels:
    {{- include "go-otel-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "go-otel-service.selectorLabels" . | nindent 4 }}
