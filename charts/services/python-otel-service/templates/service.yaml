apiVersion: v1
kind: Service
metadata:
  name: {{ include "python-otel-service.fullname" . }}
  labels:
    {{- include "python-otel-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "python-otel-service.selectorLabels" . | nindent 4 }}
